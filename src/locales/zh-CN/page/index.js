import {
    general
} from './general'
import {
    user
} from './user'
import {
    datatable
} from './datatable'
import {
    pagination
} from "./pagination"
export const page = {
    general,
    user,
    datatable,
    pagination
}